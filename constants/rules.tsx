const rules: any = {
  // DESIGN

  componentWidthPercent: '92%',
  componentWidth: 0.92,

  // USER

  passwordMinLengh: 6,
  email: /^[a-zA-Z0-9._%+-]+@(?:[a-zA-Z0-9]+\.)+[A-Za-z]{2,6}$/,
}

export default rules
